---
- hosts: localhost
  gather_facts: false
  tasks:
    # Generated by export.py
    - include_vars: "../export.yml"
    - debug:
        msg: "{{ item.value }}"
      with_dict: "{{ route53_zones }}"

    - include_tasks: test_task.yml
      with_dict: "{{ route53_zones }}"
      loop_control:
        loop_var: zones
